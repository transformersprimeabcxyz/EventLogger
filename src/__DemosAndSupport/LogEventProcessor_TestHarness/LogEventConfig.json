{
    "BufferConfig": {
        "BufferAutoFlushSize": 100,
        "FailOverSize": 200,
        "BufferSweepMs": 300,
        "CacheTimeOutMs": 400,
        "BufferWriteTimeOutMs": {
            "Default": "500",
            "ConfigKey": "Lookup Key"
        },
        "FlushFilters": [
            "Error Filter"
        ]
    },

    "PipeLine": [

        "TraceSource Writer",
        {
            "Name": "FileSystem Writer",
            "Config": {
                "sourceLevels": "Error"
            }
        }
    ],
    "Writers": [

        {
            "Name": "TraceSource Writer",
            "Type": "HashTag.Diagnostics.Writers.TraceSourceWriter, HashTag.Core"
        },

        {
            "Name": "FileSystem Writer",
            "Type": "HashTag.Diagnostics.Writers.LocalFileWriter, HashTag.Core"
        },

        {
            "Name": "LogEVentService Writer",
            "Type": "HashTag.Diagnostics.Writers.EventServiceWriter, HashTag.Core"
        }

    ],
    "Filters": [
        {
            "Nmae": "Error Filter",
            "Type": "HashTag.Diagnostics.Filters.SourceLevelFilter, HashTag.Core"
        }
    ]
}
